<%- include('../partials/header', { breaking: [], categories: [] }) %>
<div class="wrapper" style="padding:2rem 1.25rem">
  <div class="breadcrumb"><a href="/">Home</a><span class="sep">/</span><span><%= page.title %></span></div>
  <div style="max-width:860px;margin:0 auto">
    <h1 style="font-size:1.8rem;font-weight:800;margin-bottom:1.5rem;font-family:var(--font-hi)"><%= page.title %></h1>
    <div style="line-height:1.9;color:var(--text);font-family:var(--font-hi)"><%- page.content %></div>
  </div>
</div>
<%- include('../partials/footer', { categories: [] }) %>
